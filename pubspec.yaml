name: local_notes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  provider: ^6.1.5+1
  intl: ^0.20.2
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon_circular.png"
  adaptive_icon_background: "#F5F5DC"
  adaptive_icon_foreground: "assets/icons/app_icon_circular.png"

flutter:
  uses-material-design: true
